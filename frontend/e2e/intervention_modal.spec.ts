/** S44 — Intervention modal for paused runs (UX §3.7).
 *
 * Today: not implemented. Marked `test.fail` so it flips when the feature lands.
 */
import { test } from "@playwright/test";

test("S44 — paused run shows intervention modal with Continue/Review/Cancel", async () => {
  test.fail(true, "F-C-06: intervention modal not implemented in the frontend.");
});
